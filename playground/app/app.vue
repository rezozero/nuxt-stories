<template>
    <div>
        <h1>Nuxt Stories Playground</h1>
        <p>This is the main app. It can have its own pages, components, composables, plugins and CSS.</p>
        <p>All of that will be available in the stories as well, since the stories extend this app.</p>
        <p>To view the stories, run <code>npm run stories</code> and open <a href="http://localhost:6006" target="_blank">http://localhost:6006</a>.</p>
    </div>
</template>